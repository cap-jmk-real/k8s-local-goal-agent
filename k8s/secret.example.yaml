# Example — prefer scripts/apply-goal-secrets.sh (never commit real tokens).
apiVersion: v1
kind: Secret
metadata:
  name: goal-agent-secrets
  namespace: goal-workers
type: Opaque
stringData:
  GH_TOKEN: "<github-pat-for-clone-push>"
